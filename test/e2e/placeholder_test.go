//go:build e2e

package e2e_test

// TODO(TASK-0003): replace this placeholder with a real testscript harness
// (rogpeppe/go-internal/testscript) wired to cmd/nesdit, plus the
// `nesdit-idempotent` and `fake-editor` custom commands defined in
// SPEC-0001 §5. Kept here so `go build -tags e2e ./...` sees a non-empty
// package while the scaffolding lands.
